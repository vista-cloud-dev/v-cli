{
  "schemaVersion": "1.0",
  "cli": "v",
  "domains": [
    {
      "domain": "pkg",
      "version": "0.3.0",
      "contractVersion": "1.0",
      "exits": [
        {
          "code": 0,
          "meaning": "ok"
        },
        {
          "code": 1,
          "meaning": "runtime error"
        },
        {
          "code": 2,
          "meaning": "usage error"
        },
        {
          "code": 3,
          "meaning": "check / gate failed"
        },
        {
          "code": 4,
          "meaning": "refused / substrate unavailable"
        }
      ],
      "commands": [
        {
          "path": [
            "parse"
          ],
          "help": "Parse a .KID file and summarize its builds and sections.",
          "args": [
            {
              "name": "kid-file",
              "type": "string",
              "required": true,
              "help": "Path to the .KID file."
            }
          ]
        },
        {
          "path": [
            "decompose"
          ],
          "help": "Split a .KID into a per-component KIDComponents/ tree.",
          "args": [
            {
              "name": "kid-file",
              "type": "string",
              "required": true,
              "help": "Path to the .KID file."
            },
            {
              "name": "output-dir",
              "type": "string",
              "required": true,
              "help": "Output directory for the component tree (replaced if it exists)."
            }
          ]
        },
        {
          "path": [
            "assemble"
          ],
          "help": "Reassemble a component tree back into a .KID.",
          "args": [
            {
              "name": "input-dir",
              "type": "string",
              "required": true,
              "help": "Component tree (a directory of \u003cbuild\u003e/KIDComponents/)."
            },
            {
              "name": "output-kid",
              "type": "string",
              "required": true,
              "help": "Output .KID path."
            }
          ]
        },
        {
          "path": [
            "roundtrip"
          ],
          "help": "Verify decompose→assemble reproduces the build (exit 3 on drift).",
          "args": [
            {
              "name": "kid-file",
              "type": "string",
              "required": true,
              "help": "Path to the .KID file."
            }
          ]
        },
        {
          "path": [
            "canonicalize"
          ],
          "help": "Substitute install-time IENs with \"IEN\" in a tree (LOSSY; review-only).",
          "args": [
            {
              "name": "decomp-dir",
              "type": "string",
              "required": true,
              "help": "A decomposed component tree to rewrite in place (LOSSY)."
            }
          ]
        },
        {
          "path": [
            "classify"
          ],
          "help": "Derive a .KID's reversibility class (pure-overwrite vs side-effecting) from its structure — no engine.",
          "args": [
            {
              "name": "kid-file",
              "type": "string",
              "required": true,
              "help": "Path to the .KID file."
            }
          ]
        },
        {
          "path": [
            "lint"
          ],
          "help": "Run the PIKS data-class gate over a .KID (exit 3 on a blocked file).",
          "flags": [
            {
              "name": "piks",
              "type": "string",
              "help": "Path to an authoritative PIKS classification table (TSV: filenumber\u003cTAB\u003eclass)."
            },
            {
              "name": "strict",
              "type": "bool",
              "help": "Treat unclassified data files as gate failures (fail-closed)."
            }
          ],
          "args": [
            {
              "name": "kid-file",
              "type": "string",
              "required": true,
              "help": "Path to the .KID file."
            }
          ]
        },
        {
          "path": [
            "build"
          ],
          "help": "Build a KIDS transport global from a declarative build spec (deterministic, normalized export).",
          "flags": [
            {
              "name": "src",
              "type": "string",
              "default": "src",
              "help": "Directory holding the routine source (\u003croutine\u003e.m)."
            },
            {
              "name": "out",
              "type": "string",
              "help": "Output .KID path (default: dist/kids/\u003cpkg\u003e.kids)."
            }
          ],
          "args": [
            {
              "name": "spec",
              "type": "string",
              "required": true,
              "help": "Path to the kids/\u003cpkg\u003e.build.json build spec."
            }
          ]
        },
        {
          "path": [
            "install"
          ],
          "help": "Install a built .KID on a live engine over the driver (non-interactive KIDS load+install).",
          "flags": [
            {
              "name": "engine",
              "type": "enum",
              "enum": [
                "ydb",
                "iris"
              ],
              "required": true,
              "help": "Engine to reach: ydb or iris."
            },
            {
              "name": "transport",
              "type": "enum",
              "enum": [
                "local",
                "docker",
                "remote"
              ],
              "default": "remote",
              "help": "Driver transport: local | docker | remote."
            },
            {
              "name": "snapshot",
              "type": "string",
              "help": "Capture the pre-image of any routine this install overwrites to this .KID before installing (enables uninstall --restore)."
            },
            {
              "name": "auto-snapshot",
              "type": "bool",
              "help": "Like --snapshot, but to the conventional sidecar path (\u003ckid\u003e.preimage.kids) that uninstall auto-detects."
            },
            {
              "name": "allow-overwrite",
              "type": "bool",
              "help": "Overwrite existing routines WITHOUT capturing a pre-image (unsafe: uninstall cannot then restore them)."
            }
          ],
          "args": [
            {
              "name": "kid-file",
              "type": "string",
              "required": true,
              "help": "Path to the built .KID transport file to install on the live engine."
            }
          ]
        },
        {
          "path": [
            "snapshot"
          ],
          "help": "Capture the live pre-image of a patch's routines into a restorable .KID (class-1 reversal).",
          "flags": [
            {
              "name": "engine",
              "type": "enum",
              "enum": [
                "ydb",
                "iris"
              ],
              "required": true,
              "help": "Engine to reach: ydb or iris."
            },
            {
              "name": "transport",
              "type": "enum",
              "enum": [
                "local",
                "docker",
                "remote"
              ],
              "default": "remote",
              "help": "Driver transport: local | docker | remote."
            },
            {
              "name": "name",
              "type": "string",
              "help": "install name for the snapshot build (default: \"\u003corig\u003e PREIMAGE\")."
            }
          ],
          "args": [
            {
              "name": "kid-file",
              "type": "string",
              "required": true,
              "help": "the .KID about to be installed — the live pre-image of its routines is captured."
            },
            {
              "name": "out-kid",
              "type": "string",
              "required": true,
              "help": "output path for the pre-image snapshot .KID (what 'v pkg restore' re-applies)."
            }
          ]
        },
        {
          "path": [
            "restore"
          ],
          "help": "Re-apply a pre-image snapshot .KID to revert routines to stock (preview by default; --commit installs).",
          "flags": [
            {
              "name": "engine",
              "type": "enum",
              "enum": [
                "ydb",
                "iris"
              ],
              "required": true,
              "help": "Engine to reach: ydb or iris."
            },
            {
              "name": "transport",
              "type": "enum",
              "enum": [
                "local",
                "docker",
                "remote"
              ],
              "default": "remote",
              "help": "Driver transport: local | docker | remote."
            },
            {
              "name": "commit",
              "type": "bool",
              "help": "DESTRUCTIVE: install the snapshot on the live engine (overwrites the live routines). Default off = preview."
            }
          ],
          "args": [
            {
              "name": "kid-file",
              "type": "string",
              "required": true,
              "help": "the pre-image snapshot .KID to re-apply (restore to its captured state)."
            }
          ]
        },
        {
          "path": [
            "verify"
          ],
          "help": "Verify a .KID's install on a live engine (#9.7 status + per-routine presence).",
          "flags": [
            {
              "name": "engine",
              "type": "enum",
              "enum": [
                "ydb",
                "iris"
              ],
              "required": true,
              "help": "Engine to reach: ydb or iris."
            },
            {
              "name": "transport",
              "type": "enum",
              "enum": [
                "local",
                "docker",
                "remote"
              ],
              "default": "remote",
              "help": "Driver transport: local | docker | remote."
            },
            {
              "name": "drift",
              "type": "bool",
              "help": "Also check whether each shipped routine still matches the patch on the live engine (detects a later national patch overwriting it)."
            }
          ],
          "args": [
            {
              "name": "kid-file",
              "type": "string",
              "required": true,
              "help": "Path to the .KID whose install to verify (its name + routines)."
            }
          ]
        },
        {
          "path": [
            "uninstall"
          ],
          "help": "Uninstall a .KID from a live engine (routine-only back-out: routines + #9.7/#9.6).",
          "flags": [
            {
              "name": "engine",
              "type": "enum",
              "enum": [
                "ydb",
                "iris"
              ],
              "required": true,
              "help": "Engine to reach: ydb or iris."
            },
            {
              "name": "transport",
              "type": "enum",
              "enum": [
                "local",
                "docker",
                "remote"
              ],
              "default": "remote",
              "help": "Driver transport: local | docker | remote."
            },
            {
              "name": "restore",
              "type": "string",
              "help": "Pre-image snapshot .KID to restore instead of deleting (class-1 reversal for a patched-over routine)."
            },
            {
              "name": "backout",
              "type": "string",
              "help": "Authored back-out .KID to install instead of deleting (class-2 reversal of a side-effecting patch)."
            },
            {
              "name": "force",
              "type": "bool",
              "help": "Delete routines even for a side-effecting patch (UNSAFE: orphans install-time data/side-effects)."
            },
            {
              "name": "verify",
              "type": "bool",
              "help": "After a restore/back-out, confirm the live routines now match the re-applied artifact (verify-clean)."
            }
          ],
          "args": [
            {
              "name": "kid-file",
              "type": "string",
              "required": true,
              "help": "Path to the .KID whose install to reverse."
            }
          ]
        }
      ]
    },
    {
      "domain": "rpc",
      "version": "0.1.0",
      "contractVersion": "1.0",
      "exits": [
        {
          "code": 0,
          "meaning": "ok"
        },
        {
          "code": 1,
          "meaning": "runtime error"
        },
        {
          "code": 2,
          "meaning": "usage error"
        },
        {
          "code": 3,
          "meaning": "check / gate failed"
        },
        {
          "code": 4,
          "meaning": "refused / substrate unavailable"
        }
      ],
      "commands": [
        {
          "path": [
            "debug"
          ],
          "help": "Tap the RPC Broker's native XWBDEBUG log: view or save live RPC traffic."
        },
        {
          "path": [
            "debug",
            "tail"
          ],
          "help": "Stream live RPC traffic to the terminal (Ctrl-C to stop).",
          "flags": [
            {
              "name": "engine",
              "type": "enum",
              "enum": [
                "ydb",
                "iris"
              ],
              "default": "ydb",
              "help": "Engine to reach: ydb or iris ($VRPC_ENGINE)."
            },
            {
              "name": "transport",
              "type": "enum",
              "enum": [
                "local",
                "docker",
                "remote"
              ],
              "default": "docker",
              "help": "Driver transport: local | docker | remote ($VRPC_TRANSPORT)."
            },
            {
              "name": "container",
              "type": "string",
              "help": "Engine container/instance name; sets M_\u003cENGINE\u003e_CONTAINER ($VRPC_CONTAINER)."
            },
            {
              "name": "all",
              "type": "bool",
              "help": "Show every log line, not just RPC: lines."
            },
            {
              "name": "filter",
              "type": "string",
              "help": "Only RPCs whose name contains this (case-insensitive)."
            },
            {
              "name": "interval",
              "type": "float",
              "default": "1.0",
              "help": "Poll interval in seconds."
            },
            {
              "name": "duration",
              "type": "int",
              "default": "0",
              "help": "Stop after this long (e.g. 30s); 0 = run until Ctrl-C."
            },
            {
              "name": "level",
              "type": "enum",
              "enum": [
                "2",
                "3"
              ],
              "default": "2",
              "help": "XWBDEBUG level to arm: 2=names, 3=names+params (PHI)."
            },
            {
              "name": "keep",
              "type": "bool",
              "help": "Leave XWBDEBUG armed on exit (default restores the prior level)."
            },
            {
              "name": "no-clear",
              "type": "bool",
              "help": "Do not clear the existing XWBLOG on start (capture what is already buffered too)."
            },
            {
              "name": "restore-to",
              "type": "int",
              "default": "-1",
              "help": "Level to restore on exit; -1 = the level found at start. Use 1 to force stock if a prior run left it armed."
            }
          ]
        },
        {
          "path": [
            "debug",
            "capture"
          ],
          "help": "Append live RPC traffic to a file as LDJSON for offline analysis.",
          "flags": [
            {
              "name": "engine",
              "type": "enum",
              "enum": [
                "ydb",
                "iris"
              ],
              "default": "ydb",
              "help": "Engine to reach: ydb or iris ($VRPC_ENGINE)."
            },
            {
              "name": "transport",
              "type": "enum",
              "enum": [
                "local",
                "docker",
                "remote"
              ],
              "default": "docker",
              "help": "Driver transport: local | docker | remote ($VRPC_TRANSPORT)."
            },
            {
              "name": "container",
              "type": "string",
              "help": "Engine container/instance name; sets M_\u003cENGINE\u003e_CONTAINER ($VRPC_CONTAINER)."
            },
            {
              "name": "all",
              "type": "bool",
              "help": "Show every log line, not just RPC: lines."
            },
            {
              "name": "filter",
              "type": "string",
              "help": "Only RPCs whose name contains this (case-insensitive)."
            },
            {
              "name": "interval",
              "type": "float",
              "default": "1.0",
              "help": "Poll interval in seconds."
            },
            {
              "name": "duration",
              "type": "int",
              "default": "0",
              "help": "Stop after this long (e.g. 30s); 0 = run until Ctrl-C."
            },
            {
              "name": "level",
              "type": "enum",
              "enum": [
                "2",
                "3"
              ],
              "default": "2",
              "help": "XWBDEBUG level to arm: 2=names, 3=names+params (PHI)."
            },
            {
              "name": "keep",
              "type": "bool",
              "help": "Leave XWBDEBUG armed on exit (default restores the prior level)."
            },
            {
              "name": "no-clear",
              "type": "bool",
              "help": "Do not clear the existing XWBLOG on start (capture what is already buffered too)."
            },
            {
              "name": "restore-to",
              "type": "int",
              "default": "-1",
              "help": "Level to restore on exit; -1 = the level found at start. Use 1 to force stock if a prior run left it armed."
            },
            {
              "name": "out",
              "type": "string",
              "required": true,
              "help": "Output file (file://PATH or PATH); appended as LDJSON."
            },
            {
              "name": "quiet",
              "type": "bool",
              "help": "Do not also echo captured RPCs to the terminal."
            }
          ]
        },
        {
          "path": [
            "debug",
            "status"
          ],
          "help": "Show the current XWBDEBUG level and active log jobs.",
          "flags": [
            {
              "name": "engine",
              "type": "enum",
              "enum": [
                "ydb",
                "iris"
              ],
              "default": "ydb",
              "help": "Engine to reach: ydb or iris ($VRPC_ENGINE)."
            },
            {
              "name": "transport",
              "type": "enum",
              "enum": [
                "local",
                "docker",
                "remote"
              ],
              "default": "docker",
              "help": "Driver transport: local | docker | remote ($VRPC_TRANSPORT)."
            },
            {
              "name": "container",
              "type": "string",
              "help": "Engine container/instance name; sets M_\u003cENGINE\u003e_CONTAINER ($VRPC_CONTAINER)."
            }
          ]
        },
        {
          "path": [
            "debug",
            "arm"
          ],
          "help": "Turn XWBDEBUG capture on (set the broker debug level).",
          "flags": [
            {
              "name": "engine",
              "type": "enum",
              "enum": [
                "ydb",
                "iris"
              ],
              "default": "ydb",
              "help": "Engine to reach: ydb or iris ($VRPC_ENGINE)."
            },
            {
              "name": "transport",
              "type": "enum",
              "enum": [
                "local",
                "docker",
                "remote"
              ],
              "default": "docker",
              "help": "Driver transport: local | docker | remote ($VRPC_TRANSPORT)."
            },
            {
              "name": "container",
              "type": "string",
              "help": "Engine container/instance name; sets M_\u003cENGINE\u003e_CONTAINER ($VRPC_CONTAINER)."
            },
            {
              "name": "level",
              "type": "enum",
              "enum": [
                "2",
                "3"
              ],
              "default": "2",
              "help": "XWBDEBUG level: 2=names, 3=names+params (PHI)."
            }
          ]
        },
        {
          "path": [
            "debug",
            "disarm"
          ],
          "help": "Turn XWBDEBUG capture off (restore the debug level).",
          "flags": [
            {
              "name": "engine",
              "type": "enum",
              "enum": [
                "ydb",
                "iris"
              ],
              "default": "ydb",
              "help": "Engine to reach: ydb or iris ($VRPC_ENGINE)."
            },
            {
              "name": "transport",
              "type": "enum",
              "enum": [
                "local",
                "docker",
                "remote"
              ],
              "default": "docker",
              "help": "Driver transport: local | docker | remote ($VRPC_TRANSPORT)."
            },
            {
              "name": "container",
              "type": "string",
              "help": "Engine container/instance name; sets M_\u003cENGINE\u003e_CONTAINER ($VRPC_CONTAINER)."
            },
            {
              "name": "level",
              "type": "int",
              "default": "1",
              "help": "Level to restore XWBDEBUG to (1 = stock VistA default)."
            }
          ]
        },
        {
          "path": [
            "debug",
            "clear"
          ],
          "help": "Wipe the buffered XWBLOG (leave the engine pristine).",
          "flags": [
            {
              "name": "engine",
              "type": "enum",
              "enum": [
                "ydb",
                "iris"
              ],
              "default": "ydb",
              "help": "Engine to reach: ydb or iris ($VRPC_ENGINE)."
            },
            {
              "name": "transport",
              "type": "enum",
              "enum": [
                "local",
                "docker",
                "remote"
              ],
              "default": "docker",
              "help": "Driver transport: local | docker | remote ($VRPC_TRANSPORT)."
            },
            {
              "name": "container",
              "type": "string",
              "help": "Engine container/instance name; sets M_\u003cENGINE\u003e_CONTAINER ($VRPC_CONTAINER)."
            }
          ]
        },
        {
          "path": [
            "debug",
            "ping"
          ],
          "help": "Fire test RPCs at a broker so a tap has traffic to capture.",
          "flags": [
            {
              "name": "addr",
              "type": "string",
              "default": "127.0.0.1:9430",
              "help": "Broker host:port to ping; vehu=127.0.0.1:9430 ($VRPC_ADDR)."
            },
            {
              "name": "rpc",
              "type": "list",
              "help": "RPC name to send; repeatable. Default: a small no-arg set."
            },
            {
              "name": "count",
              "type": "int",
              "default": "1",
              "help": "Send the whole set this many times."
            },
            {
              "name": "timeout",
              "type": "int",
              "default": "3s",
              "help": "Per-connection read timeout."
            }
          ]
        }
      ]
    }
  ]
}
